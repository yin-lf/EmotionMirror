# EmotionMirror 测试指南

## 概述

本指南提供 EmotionMirror 项目的测试流程和测试用例，帮助开发团队验证各模块功能正确性。

---

## 测试环境准备

### 1. 启动服务

```bash
# 终端 1 - 启动后端服务
uv run uvicorn backend.app:app --reload --port 8000

# 终端 2 - 启动前端开发服务器
cd frontend && npm run dev
```

### 2. 测试工具

- **API 测试**：curl、Postman 或浏览器
- **前端测试**：浏览器（建议 Chrome/Firefox）
- **单元测试**：Python `unittest` / JavaScript `jest`（待补充）

---

## 后端 API 测试

### 测试说明

| 接口 | 方法 | 测试文件 | 预期状态 |
|------|------|----------|----------|
| `/api/health` | GET | - | 200 OK |
| `/api/text-emotion` | POST | - | 200 OK |
| `/api/voice-emotion` | POST | `backend/sample.wav` | 200 OK |

### 测试命令

```bash
# 1. 健康检查
curl http://localhost:8000/api/health
# 预期输出: {"status": "ok"}

# 2. 文本情感分析
curl -X POST http://localhost:8000/api/text-emotion \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"今天天气真好，我很开心！\"}"
# 预期输出: {"emotion": "开心", "vector": [...]}

# 3. 语音情感分析
curl -X POST http://localhost:8000/api/voice-emotion \
  -F "audio=@backend/sample.wav"
# 预期输出: {"emotion": "...", "vector": [...]}
```

---

## 前端功能测试

### 测试清单

| 测试步骤 | 功能模块 | 测试方法 | 预期结果 |
|----------|----------|----------|----------|
| 1 | 首页加载 | 访问 `http://localhost:5173` | 页面正常显示，无报错 |
| 2 | 文本输入 | 在输入框中输入文本并点击分析 | 显示情绪分析结果 |
| 3 | 语音录制 | 点击麦克风录制语音 | 录制成功并显示分析结果 |
| 4 | 头像上传 | 上传图片作为数字形象 | 图片成功上传并显示 |
| 5 | 情绪结果展示 | 查看分析后的情绪标签和向量 | 结果正确显示 |
| 6 | 步骤导航 | 点击侧边栏步骤 | 正确切换到对应步骤 |

### 测试用例

#### 用例 1：文本情感分析

**步骤**：
1. 进入「情绪输入」步骤
2. 选择「文本输入」模式
3. 输入文本：`"我今天非常高兴！"`
4. 点击「分析」按钮

**预期结果**：
- 进入「情绪分析」步骤
- 显示情绪标签为「开心」
- 显示 V-A-D 向量

#### 用例 2：语音情感分析

**步骤**：
1. 进入「情绪输入」步骤
2. 选择「语音输入」模式
3. 录制或上传一段语音
4. 点击「分析」按钮

**预期结果**：
- 进入「情绪分析」步骤
- 显示语音识别出的情绪标签
- 显示对应的情绪向量

#### 用例 3：数字形象上传

**步骤**：
1. 进入「数字形象」步骤
2. 点击上传按钮选择图片
3. 上传一张 JPG/PNG 格式的图片

**预期结果**：
- 图片成功上传
- 预览区域显示上传的图片

---

## 集成测试

### 端到端测试流程

```
用户输入 → 前端处理 → API 请求 → 后端处理 → 返回结果 → 前端展示
```

### 测试场景

**场景 1：完整工作流**
1. 启动后端和前端服务
2. 在前端输入文本进行情感分析
3. 验证后端 API 返回正确结果
4. 验证前端正确展示结果

**场景 2：错误处理**
1. 调用 `/api/text-emotion` 不传递参数
2. 验证返回适当的错误信息
3. 前端正确显示错误提示

---

## 测试检查清单

### 后端测试检查

- [ ] `/api/health` 返回状态码 200
- [ ] `/api/text-emotion` 正确处理有效输入
- [ ] `/api/text-emotion` 正确处理空输入
- [ ] `/api/voice-emotion` 正确处理音频文件
- [ ] `/api/voice-emotion` 正确处理无效文件
- [ ] 所有接口返回统一的情绪格式

### 前端测试检查

- [ ] 页面加载无错误
- [ ] 步骤导航正常工作
- [ ] 文本输入功能正常
- [ ] 语音录制功能正常
- [ ] 头像上传功能正常
- [ ] 情绪结果正确展示
- [ ] 错误提示友好显示

---

## 问题记录

在测试过程中遇到的问题请记录在此：

| 日期 | 问题描述 | 影响模块 | 状态 | 解决方案 |
|------|----------|----------|------|----------|
| 2026-05-12 | Vite 8 缺少 rolldown 绑定 | 前端 | 已解决 | 重新安装依赖 |
| - | - | - | - | - |

---

## 测试报告模板

```markdown
## 测试报告

### 测试时间
[日期]

### 测试环境
- Python: [版本]
- Node.js: [版本]
- 浏览器: [浏览器名称及版本]

### 测试结果

| 模块 | 测试用例数 | 通过 | 失败 | 跳过 |
|------|------------|------|------|------|
| 后端 API | 3 | 3 | 0 | 0 |
| 前端功能 | 6 | 6 | 0 | 0 |

### 问题汇总
- 无

### 结论
测试通过
```